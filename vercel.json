{
  "crons": [
    {
      "path": "/api/cron/bot-tick",
      "schedule": "* * * * *"
    }
  ],
  "build": {
    "env": {
      "NODE_VERSION": "20",
      "NEXT_PUBLIC_DECIBEL_NETWORK": "mainnet"
    }
  },
  "env": {
    "DECIBEL_NETWORK": "mainnet",
    "NEXT_PUBLIC_DECIBEL_NETWORK": "mainnet"
  }
}
